// // es module, most scenarios when using a bundler:
// import { api } from "zotero-api-client"; // const myapi = api("23456789").library("user", 0);
// // common-js, node and some cases when using a bundler:
// import api from "../../../react-app"

export async function ExportFeature() {
  // const response = await api()
  //   .library("user", 475425)
  //   .collections("9KH9TNSJ")
  //   .items()
  //   .get();
  // const items = response.getData();
  // console.log(items.map((i) => i.title));
}

// export default ExportFeature;
